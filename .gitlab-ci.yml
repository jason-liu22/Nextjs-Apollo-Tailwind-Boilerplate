stages:
  - prebuildcheck
  - prebuild
  - build
  - test
  - deploy

check_build_base:
  tags:
    - ubuntu
  stage: prebuildcheck
  script:
    - echo "hello world"
  only:
    - merge_requests
    - main
  allow_failure: false
